import React from 'react';

import './Header.css';

const Header = (props) => (
  <header className="Header">
    <h1>Title</h1>
    <nav>
      <a href="#">Login</a>
    </nav>
  </header>
);

export default Header;
